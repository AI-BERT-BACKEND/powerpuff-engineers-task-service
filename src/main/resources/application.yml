# ============================================================
# application.yml — single configuration file for task-service
#
# Available profiles (SPRING_PROFILES_ACTIVE):
#   inmemory  — in-memory repository, no database required (default)
#   postgres  — PostgreSQL on local Docker or Azure
#   feign     — real communication with external microservices
#
# Environment variables (only required when the corresponding profile is active):
#   DB_HOST, DB_PORT, DB_NAME, DB_USERNAME, DB_PASSWORD, DB_SSL_MODE
#   SUBJECT_SERVICE_URL, ACADEMIC_SERVICE_URL, AUTH_SERVICE_URL
# ============================================================

# -----------------------------------------------------------
# Base configuration (applies to all profiles)
# -----------------------------------------------------------
server:
  port: 8084

spring:
  application:
    name: task-service
  profiles:
    active: inmemory

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    enabled: true

feign:
  client:
    config:
      default:
        connect-timeout: 5000
        read-timeout: 10000
        logger-level: BASIC

logging:
  level:
    com.aibert.dosw.infrastructure.external: DEBUG

clients:
  academic-service:
    url: ${ACADEMIC_SERVICE_URL:http://localhost:8083}

---
# -----------------------------------------------------------
# Profile: inmemory
# Disables JPA and Flyway autoconfiguration (no database needed).
# -----------------------------------------------------------
spring:
  config:
    activate:
      on-profile: inmemory
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration

---
# -----------------------------------------------------------
# Profile: postgres
# Use for local Docker deployment and Azure.
# -----------------------------------------------------------
spring:
  config:
    activate:
      on-profile: postgres

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:taskdb}?sslmode=${DB_SSL_MODE:disable}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

---
# -----------------------------------------------------------
# Profile: feign
# Enables real communication with external microservices.
#
# Required environment variables:
#   ACADEMIC_SERVICE_URL  — base URL for academic-service (e.g. http://academic-service:8083)
#
# NOTE: task-service does NOT call auth-service.
# The API Gateway validates the JWT and injects X-User-Id header.
# -----------------------------------------------------------
spring:
  config:
    activate:
      on-profile: feign
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true

clients:
  academic-service:
    url: ${ACADEMIC_SERVICE_URL:http://localhost:8083}

feign:
  client:
    config:
      academic-service:
        connect-timeout: 5000
        read-timeout: 10000
        logger-level: BASIC


---
spring:
  config:
    activate:
      on-profile: qa
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false